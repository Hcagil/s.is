name: sis
description: "SIS — Stay In Sync."
publish_to: 'none'
version: 0.1.0-dev.1+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
